#include <bits/stdc++.h>

using namespace std;

int main(int argc, char const *argv[])
{
    int i = 3;
    // assert(3 == i);
    while (i--)
    {
        cout << "Value of i is " << i << endl;
    }

    return 0;
}